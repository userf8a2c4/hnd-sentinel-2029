 requests==2.31.0  # Para peticiones HTTP: descarga snapshots del CNE y posting a Telegram API.
-python-dateutil==2.8.2
+python-dateutil==2.8.2  # Para manejo robusto de timestamps en los snapshots.
 numpy==1.26.0
 python-telegram-bot==20.3  # Para alertas automáticas en Telegram.
+tweepy==4.14.0  # Para posting en X (Twitter).
 requests-oauthlib==1.3.1  # OAuth 1.0a for X API.
+apscheduler==3.10.4  # Scheduler para ejecuciones horarias del pipeline.
 # The Sentinel's arsenal for truth exposure. No libs extras para mantener ligereza.
